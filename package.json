{
  "name": "@hidden-identity/root",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "packageManager": "pnpm@10.6.5",
  "license": "GPL-3.0",
  "engines": {
    "node": "18.20.2"
  },
  "scripts": {
    "e2e-test": "pnpm run --stream -r e2e-test",
    "dev": "pnpm run --stream -r --parallel dev",
    "build-client": "pnpm run --stream --filter @hidden-identity/client... build-client",
    "prepare": "husky install",
    "type-check": "pnpm run --stream -r type-check",
    "test": "pnpm run --stream -r test"
  },
  "keywords": [],
  "author": "",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^14.0.1"
  }
}
